// Import necessary dependencies
import React from 'react';
import MainContainer from './navigation/MainContainer';

// Create the root component
export default function App() {
  return (
      <MainContainer>
        {/* Your other navigation components */}
      </MainContainer>
  );
}